class Tuple {
    public:
        float x;  // Attribute
        float y;  // Attribute
        float z;  // Attribute
        float w;  // Attribute
        Tuple(float x, float y, float z, float w);
        bool isPoint();
        bool isVector();
};
